[package]
name = "nac3standalone"
version = "0.1.0"
authors = ["M-Labs"]
edition = "2024"

[features]
no-escape-analysis = ["nac3core/no-escape-analysis"]

[dependencies]
parking_lot = "0.12"
nac3core = { path = "../nac3core" }

[dependencies.clap]
version = "4.5"
features = ["derive"]
