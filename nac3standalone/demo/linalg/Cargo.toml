[package]
name = "linalg"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
nalgebra = { version = "0.33", default-features = false, features = ["libm", "alloc"] }

[workspace]
